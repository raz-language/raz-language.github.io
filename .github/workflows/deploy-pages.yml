name: deploy-pages
on:
  push:
    branches: [main]
  workflow_dispatch:
  schedule:
    - cron: '17 */6 * * *'
permissions:
  contents: read
  pages: write
  id-token: write
# Never let a delayed/stale run cancel a newer successful Pages build.
# Stale commits are detected explicitly below and exit without deploying.
concurrency:
  group: pages
  cancel-in-progress: false
jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      should_deploy: ${{ steps.freshness_final.outputs.current }}
    steps:
      - uses: actions/checkout@v4
      - name: Check whether this run is still current
        id: freshness
        shell: bash
        run: |
          latest="$(git ls-remote origin refs/heads/main | awk '{print $1}')"
          echo "workflow sha: $GITHUB_SHA"
          echo "current main: $latest"
          if [ "$GITHUB_SHA" = "$latest" ]; then
            echo "current=true" >> "$GITHUB_OUTPUT"
          else
            echo "current=false" >> "$GITHUB_OUTPUT"
            echo "This is a stale workflow run; build and deployment will be skipped."
          fi
      - uses: actions/setup-python@v5
        if: steps.freshness.outputs.current == 'true'
        with:
          python-version: '3.12'
      - name: Refresh canonical Raz data and documentation
        if: steps.freshness.outputs.current == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RAZ_SITE_URL: ${{ vars.RAZ_SITE_URL }}
        run: python3 scripts/sync_site.py --refresh
      - name: Run generator regression tests
        if: steps.freshness.outputs.current == 'true'
        run: python3 scripts/test_sync_regressions.py
      - name: Check release/news publication
        if: steps.freshness.outputs.current == 'true'
        run: python3 scripts/test_release_news.py
      - name: Check official release distribution
        if: steps.freshness.outputs.current == 'true'
        run: python3 scripts/test_release_distribution.py
      - name: Check package search contract
        if: steps.freshness.outputs.current == 'true'
        run: python3 scripts/test_package_search.py
      - name: Check versioned documentation snapshots
        if: steps.freshness.outputs.current == 'true'
        run: python3 scripts/test_versioned_docs.py
      - name: Check package product UX
        if: steps.freshness.outputs.current == 'true'
        run: python3 scripts/test_package_product_ux.py
      - name: Check lazy global search
        if: steps.freshness.outputs.current == 'true'
        run: python3 scripts/test_lazy_search.py
      - name: Check browser asset cache keys
        if: steps.freshness.outputs.current == 'true'
        run: python3 scripts/test_asset_cache_busting.py
      - name: Check generated performance budget regression
        if: steps.freshness.outputs.current == 'true'
        run: python3 scripts/test_performance_budget_generation.py
      - name: Check accessibility contract
        if: steps.freshness.outputs.current == 'true'
        run: python3 scripts/test_accessibility_contract.py
      - name: Check production metadata
        if: steps.freshness.outputs.current == 'true'
        run: python3 scripts/test_production_metadata.py
      - name: Check production-origin fallback
        if: steps.freshness.outputs.current == 'true'
        run: python3 scripts/test_site_origin_fallback.py
      - name: Check v24 correctness hardening
        if: steps.freshness.outputs.current == 'true'
        run: python3 scripts/test_v24_hardening.py
      - name: Check page layout hierarchy
        run: python3 scripts/test_v25_layout_hierarchy.py
      - name: Check Markdown token rendering
        run: python3 scripts/test_markdown_tokens.py
      - name: Check homepage hero
        run: python3 scripts/test_homepage_hero.py
      - name: Check v26 product content
        if: steps.freshness.outputs.current == 'true'
        run: python3 scripts/test_v26_product_content.py
      - name: Check package version routes
        if: steps.freshness.outputs.current == 'true'
        run: python3 scripts/test_v27_package_versions.py
      - name: Check responsive/reference usability
        run: python3 scripts/test_v28_responsive.py
      - name: Check evidence and discovery
        run: python3 scripts/test_v29_evidence_discovery.py
      - name: Check CLI and contributor surfaces
        if: steps.freshness.outputs.current == 'true'
        run: python3 scripts/test_v30_developer_surfaces.py
      - name: Validate footer balance
        if: steps.freshness.outputs.current == 'true'
        run: python3 scripts/test_v30_1_footer_balance.py
      - name: Validate v31 navigation and README
        if: steps.freshness.outputs.current == 'true'
        run: python3 scripts/test_v31_navigation_readme.py
      - name: Validate generated source tree
        if: steps.freshness.outputs.current == 'true'
        run: python3 scripts/validate.py
      - name: Check performance budgets
        if: steps.freshness.outputs.current == 'true'
        run: python3 scripts/check_performance.py
      - name: Stage public website
        if: steps.freshness.outputs.current == 'true'
        run: python3 scripts/stage_site.py --output _site
      - name: Check deploy performance budgets
        if: steps.freshness.outputs.current == 'true'
        run: python3 scripts/check_performance.py --root _site
      - name: Validate deploy artifact
        if: steps.freshness.outputs.current == 'true'
        run: python3 scripts/validate.py --root _site
      - name: Confirm this run is still current before upload
        id: freshness_final
        if: steps.freshness.outputs.current == 'true'
        shell: bash
        run: |
          latest="$(git ls-remote origin refs/heads/main | awk '{print $1}')"
          echo "workflow sha: $GITHUB_SHA"
          echo "current main: $latest"
          if [ "$GITHUB_SHA" = "$latest" ]; then
            echo "current=true" >> "$GITHUB_OUTPUT"
          else
            echo "current=false" >> "$GITHUB_OUTPUT"
            echo "A newer main commit appeared while this run was building; artifact upload will be skipped."
          fi
      - uses: actions/configure-pages@v5
        if: steps.freshness_final.outputs.current == 'true'
      - uses: actions/upload-pages-artifact@v3
        if: steps.freshness_final.outputs.current == 'true'
        with:
          path: _site
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    if: needs.build.outputs.should_deploy == 'true'
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
